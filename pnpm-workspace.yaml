packages:
  - 'apps/*'
  - 'packages/*'

# @sentry/cli's postinstall downloads its own CLI binary (used for source-map
# upload at build time). Official first-party Sentry package, known/expected
# postinstall — approved to run. Key/format per this pnpm version's own
# auto-generated suggestion (pnpm 11.8.0).
allowBuilds:
  '@sentry/cli': true
